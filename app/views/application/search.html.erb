<div class="hero-unit">           
    <div class="navbar">
        <form class="navbar-form" method="get" action="/submit.html/">
            <input name="query" class="span6 input-large" type="text" placeholder="Search String..." value="<%= @query %>">
            <button type="submit" class="btn btn-large">Go</button>
        </form>
    </div>
</div>

<% 0.upto(@output.length - 1) do |i| %>
  <div class="row">
    <div class="span1"><img src="http://cdn.stoychev.info/icons/index/<%= @output[i].split('.')[-1].downcase %>.png" width="24" height="24" alt="<%= @output[i].split('.')[-1].downcase %>" /></div>
    <div class="span11">
      <ul class="breadcrumb">
      <%  
        chunks = file.split('/');
        chunks.each do | path_chunk, i |
      %>        
        <%   
          path_updirs = file.split(path_chunk)[0];
          path_updirs_urlencoded = ""
          if !path_updirs.nil?
            path_updirs.split('/').each do |updir_chunk| 
               path_updirs_urlencoded = path_updirs_urlencoded + '/' + u(updir_chunk)
            end
          end
        %>
        
        <% if i == chunks.length - 1 %>
            <li><a href="/browse.html<%= path_updirs_urlencoded + '/' + u(path_chunk) %>"><%= path_chunk %></a> <span class="divider">/</span></li>
        <% elsif %>
            <li><a href="/browse.html<%= path_updirs_urlencoded + '/' + u(path_chunk) %>/"><%= path_chunk %></a> <span class="divider">/</span></li>
        <% end %>
      <%  0.upto(@output[i].split('/').length - 2).each do | n | %>
            / <a href="/browse.html/<%= @output_urlencoded[i].split('/')[0..n].join('/') %>/"><%= @output[i].split('/')[n] %></a>
      <% end %>

      </ul>
       / <a href="/music/<%= @output_urlencoded[i] %>"><%= @output[i].split('/')[-1] %></a>
    </div>
  </div>
<% end %>
